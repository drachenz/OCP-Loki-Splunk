apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production environment overlay (not yet configured).
#
# Before use, mirror the structure of ../test:
#   - cluster settings (Splunk URL, StorageClass) and replacements
#   - Secrets logging-loki-s3 and splunk-hec; for GitOps, consider
#     Sealed Secrets or the External Secrets Operator
#   - LokiStack size is inherited from base (1x.small)
#
# If Splunk presents a certificate from a private CA, add a tls.ca reference
# to the Splunk output instead of disabling verification.

resources:
  - ../base
